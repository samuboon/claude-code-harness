**English: [README.md](README.md)**

# claude-code-harness —— 4 日弱で 150 回、AI が予算を残したまま終わろうとしたので、機械の門を作った

**この道具は、エージェントを毎日 `/loop` で無人自走させている運用で、実際に使っているものです。**下の数字はすべてその運用の実測で、都合の悪いものも書いています。

Claude Code を毎日 `/loop` で自走させると、**予算(呼び出し回数・時間)が半分以上残っているのに、AI が自分で「今日はここまで」と判断してターンを終える**ことが繰り返し起きます。ここに置いたのは、その事故を機械で捕まえるために作った 6 本のスクリプトです。**Python の標準ライブラリだけで動きます**(依存パッケージ 0)。

---

## 実測(誇張なし。都合の悪い数字も書きます)

| 何を | いくつ |
|---|---:|
| **門が発火した記録(`RUNS.tsv`。2026-09-10 16:37 〜 09-16 = 6 日間)** | **423 行** |
| **うち、自走中(錠が生きている状態)の発火** | **151 回** |
| **→ 予算が残っているとして、ターンを続けさせた** | **150 回**(09-10: 1 / 09-11: 14 / 09-12: 39 / 09-13: 43 / 09-14: 53) |
| **→ 予算切れとして通した** | **1 回**(09-11 09:42・`T=8/8`。この出口はその後に消した。下の「分かったこと 5」) |
| 錠が無く素通りした(自走でないターン) | 272 回 |
| **09-15・09-16 の発火のうち、門が武装していた回数** | **84 回中 0 回** —— 計器が消えた。次の節 |
| そのうち**誤作動** | **1 回**(失効した錠を生きていると誤判定した。修正済み。上の 09-10 の 1 回がこれ) |
| 規約(自然言語のルール)を足して直そうとした回数 | **2 回** —— **2 回とも次を防げなかった** |
| **門が自分を通した「8 回で止まってよい」に根拠があったか** | **無かった**(4 時間の予算のうち 1 時間 53 分で通した。出口から回数を外した。下の「分かったこと 5」) |
| 同梱の検査(2026-09-16 に実行) | **6 ファイル 38 本すべて成功・失敗 0** |

**この表は `state/RUNS.tsv` を数え直した値で、最終更新は 2026-09-16 です。**同梱の雛形と同じ列なので、手元でも同じ数え方ができます。

**書いていない数字も書きます。**①**150 回すべてが「早すぎる停止」だとは言えません** —— 錠のラベルから全件が自走セッション中だとは確認できますが、**自走中でも人に報告して区切ることはあり、門はその区別をしません**(区別する計器はまだありません)。②**「止めた先で良い仕事が出たか」は自動では測れていません** —— 手で追える範囲では、続けた先で欠陥の修正・棚の実測・競合の観察が出ています。③この表の最初の版は「本番で効いた回数 0 回」でした。効いた数字と効かなかった数字を両方書きます。そう書いていない同種の道具を見かけたら、その数字を聞いてみてください。

---

## 自走の形を変えた日(2026-09-15)に、計器が消えました

09-15 に自走の回し方を変えました。1 つの長いセッションが待ち行列を上から片付ける形をやめ、**本線が 1 単位ずつ、文脈を持たない作業者(サブエージェント)に渡す形**にしたのですが、**作業者には Stop hook がありません。**

門は本線では今も発火しています —— **09-15 に 62 回・09-16 に 22 回。**ただし、そのターンは門が見ている自走の錠を持たないので、**84 回中 0 回しか武装せず**、全部が素通りしました。検査は 1 本も落ちず、エラーも出ません。**見張っている対象の形が変わった日に、検査がただ当たらなくなっただけ**です。

**だから「この門が効いているか」を示す数字は、発火回数ではなく武装回数です。**84 回発火して 1 回も止めない門は、発火回数だけを見るどんな画面の上でも、「早く終わろうとしないセッションを見ている門」と見分けがつきません。気づけたのは、自走が走り続けているのに上の表の日別の列が 0 になったからです。

**Stop hook を使っている方へ: あなたの門は何回発火して、そのうち何回が武装していましたか。**2 つの数字と、その期間だけで結構です。[このスレッドに貼ってください](https://github.com/samuboon/claude-code-harness/discussions/3) —— 1 つのリポジトリの記録は、まだ数字ではありません。(2026-09-17 まで、この文は「そのスレッドで集計を続けます」と書きながら議論の**索引**を指していました。集計するスレッドは存在しませんでした。下の 4 を参照。)

---

## この棚自身の数字 —— 2 週間で来たのは 3 人、道具のフォルダを開いた人は 0 人(2026-09-16 実測)

道具の出来を測る前に、**そもそも人が来ているのか**を数えました。GitHub の Traffic API が返した、このリポジトリ自身の数字です。**窓は API が返したままで、2026-09-01 から 09-14 の 14 日間**(09-15・09-16 はまだ返りません)。

| 何を | いくつ |
|---|---:|
| 閲覧 | **5 回 / のべ 3 人** |
| 外部からの流入元(referrer) | **0 件** |
| 開かれたページ | Overview が 4 回(3 人)と Discussion が 1 回(1 人)。**それ以外は 0** —— この窓の間、**道具のフォルダは 1 つも開かれていません** |
| clone | 70 回 / 30 人。ただし**全部が公開直後の 2 日に集まっています**(09-12 に 23 人・09-13 に 12 人)。**以後の 12 日間は毎日 0** |

**3 人のうち何人が自分たちかは分かりません**(09-12 はこのリポジトリを作った日です)。**いま在るフォルダの多くはこの窓より後に置いたので、その分はまだこの数字に入っていません。**次に同じ 4 行を取るのは 09-29 です。

ここから 3 つ分かって、そのうち 2 つは自分たちの計器を壊しました。

1. **star が 0 なのは、道具が要らなかったからではありません。開かれていないからです。**中身の良し悪しは、まだ一度も測られていない。**同じ場所に次の 1 本を足しても、同じ 0 になります**
2. **「clone した人数」を反応として数えるのは間違いでした。**30 人は公開直後の 2 日だけに現れ、以後 12 日 0 —— これは人の関心の形ではなく、**新しい公開リポジトリを写す機械の形**です。自分たちの反応の判定からこの列を外しました
3. **置いた数は、流入が 0 の場所では効きません。**この 2 週間で置いた本数は、上の表を 1 も動かしていません
4. **こちらから出していた「頼み」は 21 個あり、返ってきたものは 0 件でした。**2026-09-17 に数え直しました —— 読者に「貼ってください」「Issue に書いてください」と頼んでいる文は、このリポジトリ全体で **21 個**(日本語のページに 10・英語のページに 11)。同じ日の実測で、**Issue は開いた分も閉じた分も合計 0 件**、議論の 3 本はすべてこちらが立てたもので、付いた書き込み 3 件と賛成 3 件もこちらとオーナーの口座だけです。同じ日に `/traffic/popular/paths` を取り直しても、返った経路は **Overview(5 回 / 4 人)と Discussion 1 本(1 回 / 1 人)の 2 つだけ** —— つまり **21 個のうち 17 個は、1 度も開かれていないページの上に載っています**。そして**唯一開かれているこの Overview に載っている 4 個のうち、書いてあるとおりの場所に着くのは 1 個だけでした**: 1 つは「そのスレッドで集計を続けます」と書きながら、そのスレッドが存在しない。1 つは「Issue に貼ってください」のリンク先が投稿口ではなく自分たちの説明ページ。1 つは、そのために作った[雛形のファイル](.github/ISSUE_TEMPLATE/expiry-report.yml)がリポジトリの中に置いてあるのに、**木の中のどこからもリンクされていませんでした**。**3 つとも今日直しました。**ついでに、ログアウトした状態で `issues/new` と `issues/new/choose` を開いて確かめたところ、**投稿の口はどちらもサインインの画面に出ます**(公開リポジトリでも、読むのは誰でも・書くには口座が要る)。**つまり頼みの手数は、こちらが書いた 1 文ではなく、読者の側の 5 手です** —— 来る・そのページを開く・走らせる・口座を持っている・貼る。上の表が言っているのは、**1 手目がまだ 0 だ**ということです。頼みの数を増やすことと、頼みが着く先を用意することと、1 手目を 0 でなくすることは、それぞれ別の仕事です

**同じ 4 行は、あなたの公開リポジトリでも取れます** —— `/repos/<あなた>/<リポジトリ>/traffic/views` と `/traffic/popular/paths`(自分のリポジトリなら push 権のある鍵で読めます)。**star の数ではなく「開かれたページ」と「流入元」を見てください。**その 2 つが 0 なら、直すのは中身ではありません。[その 4 行を Issue に貼ってもらえると](https://github.com/samuboon/claude-code-harness/issues/new)、1 つのリポジトリの数字が分布になります。

---

## ここへ来る索引で順位を取りにいって、勝っている側の読者を数えたら 0 人でした(2026-09-17 実測)

上の節は「1 手目がまだ 0 だ」で終わっています。誰も来ない。直す先として素直なのは索引です。star 0 のリポジトリが届ける入口は議論の検索しか残っていないので、3 日かけて議論 2 本の題名を、読者が実際に打つ言葉に書き換えました。**順位は動きました** —— 1,196 件の索引で 23 位 → 6 位、別の語では 50 位圏外 → 1 位。**流入は 1 も動きませんでした。**

そこで順位を取りにいくのをやめ、**すでに自分の上に居るページのほうを数えました。**

**数え方。**`claude code <語>` の形で 9 語、それぞれ上位 20 件。延べ 173 件、重複と自分を除いて **148 件**。1 件ごとにリポジトリの star・分類・返信数・賛成数・質問に回答が付いているかを取りました。

| 何 | 件数 |
|---|---:|
| 自分の周りか上に並んでいた議論(実体) | **148** |
| うち返信 3 件以上 | **30 件(20%)** |
| うち回答が付いているもの | **30 件中 1 件** |
| star の中央値 —— 返信が付いた側 / 全体 | **3,275 / 5,142**(star ではほとんど分かれない) |
| 返信が付いた側のうち、star 1,000 未満のリポジトリ | **30 件中 13 件(43%)** |

この表だけなら「順位は star で買われていない」という明るい読み方になります。**返信を書いているのが誰かを数えたところで、明るくなくなりました。**

**上の 30 件のうち、返信が多い 8 件について、書き込みの投稿者が何人かを数えました。**

| 議論 | 返信 | 投稿者の人数 | 最多の投稿者の占有率 |
|---|---:|---:|---:|
| `elizaOS/eliza`「Agent Fleet HQ v2 — coordination room」 | 1,815 | **2 人**(100 件の標本) | 91% |
| `Skitchy/Drinking-Water-Treatment-Corpus`「Architecture discussion」 | 123 | **1 人**(100 件の標本) | 100% |
| `GlomarGadaffi/pocket-dial`「Session status board」 | 63 | **1 人** | 100% |
| `dezeat/claude-usage-meter`「Session handovers — running log」 | 20 | **1 人** | 100% |
| `neomjs/neo`「[Ideation Sandbox] …」 | 44 | 5 人 | 30%(`neo-gpt`) |
| `MadsLorentzen/ai-job-search`「Community forks & adaptations」 | 39 | **34 人** | 10% |
| `aaif-goose/goose`「ACP provider support in Goose」 | 7 | **7 人** | 14% |
| `agentskills/agentskills`「benchmark.json cannot be int…」 | 4 | 3 人 | 50% |

**この索引でいちばん返信が多い 8 件のうち 4 件は、1 つの口座が自分宛てに書いているものでした。**艦隊の連絡室・設計の独り言・夜ごとの状況ボード・引き継ぎのログ —— エージェントが議論を書き置き場として使っていて、返信の数え上げがその 1 行 1 行を律儀に数えています。見分けがつく印は隣の列にあります: **返信 1,815 件に対して、賛成 1 票。**

人が集まっていたのは 8 件中 2 件だけで、その 2 件は互いに同じ形をしています。**大きなリポジトリ**(star 43,000 と 54,000)の、**読者が自分の成果を書き込む場**です。「Community forks & adaptations」—— 34 人。

**だから、分かったのはこれです: 返信の数は読者が居る印ではありません。**代わりに**投稿者の人数**を数えてください。人と同じだけエージェントが書く場所では、この 2 つの数はすでに別物になっていて、3 日かけて追いかけた順位は、意味を失ったほうの数の上に建っていました。

**言えないこと。**8 件は 30 件の中で**返信が多い側**であって無作為の標本ではありません —— 分布の下のほうでも同じ形かは示せていません。投稿者は 1 本につき先頭 100 件までの標本なので、`elizaOS` の 91% は 1,815 件に対してではなく 100 件に対してです。`neo-gpt`・`neo-opus-vega` は名前からエージェントの口座に見えますが、確かめていません。9 語はすべて `claude code <語>` の形なので、ここの順位は**すでに「claude code」と打った人の中での順位**です —— 語だけで測り直すと、4 桁の索引 5 つのうち 4 つは 50 位圏外でした。信じる前に通した対照は 2 つ: 当たってはいけない語(`kubernetes ingress nginx tls`・800 件)で自分は圏外、当たるべき語(`claude code denied by classifier`・31 件)で自分が 1 位(過去 3 日の実測と一致)。

**この 1 つは、あなたが数えている面にもそのまま移ります。**賑わって見えるスレッドを取り、書き込みの投稿者を引き出して、割ってください。1 つの名前が大半を占めていたら、その数字は最初から読者ではなく、誰かのログでした。

---

## 何が入っているか

### `.claude/hooks/stop_gate.py` —— ターン終端の門(Stop フック)

AI が「作業を終えます」と言った瞬間に必ず走ります。**呼ぶかどうかを AI 自身が決められない**のがこの道具の全部です。予算(呼び出し回数 T / 経過時間 / 自動圧縮の回数)を機械で数え、残っていれば `exit 2` を返して**ターンを続けさせます**。

- 発火のたびに `RUNS.tsv` に 1 行(いつ・自走中か・T・判定・落ちた検査)を残す。**この記録が上の表です**
- 止めるとき AI に返す文は**上流から**: ①ゴール(STATUS の目標と距離)②財布と今週の市場接触 ③「いまの計画で届くか / 届かないなら何が足りないか / それを埋める仕事は待ち行列にあるか」の 3 問 ④待ち行列の次 ⑤予算(参考)。**初版は「次のタスク」から返していて、止められた AI は 7 回とも次のタスクを取り、上流には戻らなかった**(間隔 21 → 2 分)。人間なら止まる前にゴールに戻る、という指摘で変えた
- **自走中(専用の錠がある)にだけ効く。**人と対話しているセッションでは素通りする
- **注意**: この設計は 1 度誤作動しました。錠の鮮度を見ずにラベルの文字列だけを読んでいたため、失効した錠で対話ターンを止めました。修正済み(`tools/test_stop_gate.py` に回帰検査 2 本)

### `.claude/hooks/guard.py` —— 権限ガード(PreToolUse フック)

**プロンプトインジェクションとサプライチェーンへの防衛を、AI の判断より手前に置く**ための道具です。

**v2(2026-09-12)。**初版は「書き込み先」と「導入・実行」しか見ておらず、自分で敵対的にレビューしたら **24 経路で破れました**(Bash 経由で自分の解錠ファイルを作れる・個人の Chrome を操作できる・プロジェクト外のファイルを読める、など)。v2 は前提を変えています —— **エージェントが取得物の指示に従ってしまっても、①防御を外せない ②鍵を外に出せない ③プロジェクト外(私用端末)を読めない ④オーナーの識別子を外に書けない。**

- **見るツール**: Edit / Write / Read / Glob / Grep / Bash / WebFetch / WebSearch / Artifact / SendUserFile / MCP 全部(登録は `templates/settings.json`)
- **止めるもの**: プロジェクト外への書き込み**と読み取り** / 導入・起動 / パイプからシェルへの流し込み・eval・base64 復号・EncodedCommand / ssh・scp / 端末の設定変更(setx・reg・schtasks・`git config --global`)/ **鍵そのもの(`github_pat_` 等)がどのツールの引数にも現れること** / **許可リスト(`.claude/allowed_hosts.txt`)に無い宛先へ送ること**(宛先が文字列から読めない送信も)/ **防御ファイル自身の改変**(解錠は人間が `.claude/unlock.md` を手で作ったときだけ。AI はどの経路からも作れず消せない)
- **止めないもの**: 読むこと(https)。**Python も封じません** —— その場のコード(`python -c` / ヒアドキュメント)の中の通信・鍵の読み出し・起動だけを見ます。止めるときは「どうすれば通るか」を 1 行返します
- **誤検知の測り方**: 直近セッションの実コマンドを全部このフックに流して数えます。本番では **942 件中 誤検知 1 件(0.1%)** まで絞ってから入れました(初版の直しは 8.6% を止めていました)。検査は `tools/test_guard.py`(19 本)
- **記録**: 拒否・外部送信・外部取得を `private/guard.log` に 1 行(鍵はマスク)
- **設定 3 つ**: `.claude/allowed_hosts.txt`(送ってよいホスト。**空なら送信は全部拒否**)/ `.claude/hooks/private_patterns.txt`(オーナーの識別子。任意)/ `guard.py` の `PROJECT_SESSION_KEY`(セッション記録のディレクトリ名に含まれる語)
- **既知の穴**: 意図的な難読化(`'cu'+'rl'` のような文字列の分割)は正規表現では捕まりません。乗っ取られたエージェントが書くコードは指示どおりの素直な形になるので、パターンで効きます —— 難読化を指示する文自体が「取得物の中の指示」として弾く対象です。初版の穴(`WebFetch` の保存を見ていない)は v2 で閉じました

### `.claude/hooks/compact_count.py` —— 自動圧縮を数える(PreCompact フック)

自動圧縮が走るたびに同じ帳簿へ記録します。2 回で「文脈を 2 度失った」ことになり、予算の条件の 1 つになります。**圧縮そのものは決して止めません。**

### `tools/session_lock.py` —— 同時実行の錠

同じプロジェクトで自走を 2 本走らせると帳簿が壊れるので、錠を 1 本に絞ります。TTL(既定 4 時間)で失効し、古い錠は乗っ取れます。`stop_gate` はこの錠を見て「自走中か」を判定します。

### `tools/stop_check.py` —— 予算の判定(門の中身)

`RUN.md`(圧縮回数)と `STATUS.md` と待ち行列を読み、**止めてよいかを 3 つの条件だけで判定**します —— 4 時間経過・自動圧縮 2 回・45 分無更新。「品質が落ちた気がする」「手段が無い」のような、AI がいくらでも当てはめられる理由は**意図的に外してあります**(1 度入れて、口実に使われたので消しました)。

### `tools/contacts.py` —— 「外に置いたもの」の帳簿

AI に事業をやらせると、**文書と分析だけが増えて、見知らぬ人の前には何も置かれない**状態になります。置いた 1 件ごとに TSV へ 1 行残し、週の件数と「置いた数あたりの反応」で経路を並べます。

### `tools/linkcheck.py` —— 相対リンクの検査

文書を移動・改名したあとの壊れリンクを数えます。おまけで「1 行が長すぎる表の行」も数えます(AI に台帳を書かせると 1 行が際限なく伸びるため)。

### `examples/refill-loop/` —— ループが何も生まないもう 1 つの道: 行列が空になる朝

上の門は「予算を残したまま終わろうとする AI」を止めますが、**待ち行列に今日取れる行が 1 本も無い朝**には効きません。落ちもせず、拒否もされず、やることが無いと言って、12 時間戻らない人間を待ちます。私たちはこれである朝の **110 分のうち 57 分**を失いました。[`examples/refill-loop/`](examples/refill-loop/) に、その **2 つの答えの両方**の最小実装を置いてあります —— 前夜に検知する(`queue_forecast.py`)と、止まらずその場で補充する(`refill_loop.py`)。実測と、まだ決着していない割れも一緒に。試験 19 本。

### `pjo-exit-check/` —— 期限のある単位を、使える道具の形で出したもの

エージェントの話ではありません。ここに置いてあるのは、ループに**決まった日に価値が消える仕事**が 1 つ必要で、実在する期限が人を動かすのかを見たかったからです。Microsoft Project Online は **2026-09-30 8:00:00 AM 太平洋時間**で終わります。公式の退避スクリプトは一覧表を出さないので、「終わった」と「揃っている」は別の話です。[`pjo-exit-check/`](pjo-exit-check/) は、書き出し先のフォルダに公式文書のファイルがどれだけ欠けているかを数えます。そして **Microsoft 自身のページが 2 通りに綴っている 6 つのファイル名**を両方受け付けます —— 手書きの点検表が「無い」と誤判定するのは、まさにここです。試験 27 本、わざと壊した 7 通りをすべて捕捉。テナントを持っていないので実物に対しては 1 度も走っておらず、README の最初にそう書いてあります。

### `abandoned-demand/` —— `not planned` で閉じられた要望に付いたままの 👍、合計 35,206

下のプラグインの節の末尾にある脚注 —— 58 👍 が付いたまま `wontfix` で閉じられた要望 —— を、そのまま一般化したものです。[`abandoned-demand/`](abandoned-demand/) は、開発元が `not planned` で閉じた・`wontfix` を付けた Issue を集め、**👍 だけ**で並べます。**GitHub は 👍 で並べ替えも絞り込みもできない**からです。検索の `reactions:` は 😕 と 👎 も数えており、今日の実測でその差は**返ってきた 121 件のうち 13 件**でした。いまの一覧は [`MAP.md`](abandoned-demand/MAP.md) の **108 件・👍 合計 35,206**。試験 42 本、わざと壊した 17 通りをすべて捕捉、標準ライブラリのみ。Issue の本文は 1 行も読まず、題が読み手に話しかけることも許しません（最初に生成した一覧を自分たちの公開前検査が突き返したのが、その規則の発端です）。「月 1 回更新」と言っている一覧の 1 回目です。

### `leak-scan/` —— 下にある `.pyc` の失敗を、そのまま GitHub Action にしたもの

下の「この木を公開する前に消すもの」は、2 つの計器が同じ死角を共有していた話です。`.pyc` は git に追跡されず、しかもバイナリなので、`git status` にも文字列の grep にも、中に入っていた開発機のユーザー名が見えませんでした。[`leak-scan/`](leak-scan/) は、そのとき持っているべきだった検査です。**git の索引ではなくディレクトリを歩く**ので未追跡のファイルも読み、探す語を **UTF-8 と UTF-16LE の両方**で照合します。バイナリに埋め込まれた文字列は実際にその形で入っているからです。

**1 件も読まなかった走査を合格にしません**(終了コードは 0 ではなく **2**。経路を打ち間違えただけで、検査していない木に緑の印が付くのを防ぐため)。見つけた文字列は**伏せて**報告します。公開リポジトリの CI ログは公開なので、見つけたものをそのまま出す検査は、漏れを捕まえたのではなく移しただけになります。

リポジトリ直下に [`action.yml`](action.yml) があるので、そのまま 1 ステップとして使えます。

```yaml
- uses: samuboon/claude-code-harness@main
  with:
    patterns: .github/my-identifiers.txt   # 任意: 自分の名前やユーザー名の一覧
```

試験 37 本、わざと壊した 16 通りをすべて捕捉、標準ライブラリのみ。誤検知の実測と、**できないこと**の一覧は [`leak-scan/README.ja.md`](leak-scan/README.ja.md) にあります。

### `key-expiry/` —— この端末が信頼している根証明書 48 枚のうち、10 枚は既に期限切れだった

`leak-scan/` は木の中の資格情報を見つけます。[`key-expiry/`](key-expiry/) は**それがいつ死ぬか**を、通信なしで言います —— 自分の死ぬ日を自分の中に持っている資格情報は **2 種類だけ**だからです。JWT の `exp` クレームと、X.509 証明書の `notAfter`。それ以外(GitHub の個人アクセストークン、AWS のキー)は日付を一切含まない文字列で、この道具は推測せずに「読めない」と書きます。

読み進める前に `python key-expiry/trust_store_demo.py` を走らせてください。OS が既に持っている証明書だけを読みます。この端末の 2026-09-16 の実測は **48 枚中 10 枚が期限切れ、最も古いものは 9,757 日前**。期限切れの根は穴ではありません(何も検証できないだけです)。そこが要点で、**その日付はずっと平文でディスクの上にあり、誰も読んでいなかった**のです。

種類と残り日数だけを出し**パスを出さない** `--format paste` を持っています。この手の数字が共有されないのは、期限の検査器の自然な出力が「自分の端末のディレクトリの一覧」だからです。**走らせたら、その塊を [Issue の雛形](https://github.com/samuboon/claude-code-harness/issues/new?template=expiry-report.yml)に貼ってください** —— 1 台の端末の証明書置き場は分布ではありません。

**この道具は、こちらが実際に踏んだ事故を捕まえられません。**README はそれを何よりも先に書いています —— ここで落ちた push が落ちたのは**権限が 1 つ足りなかった**からで、日付ではなく、権限は鍵の中に一切書かれていません。試験 50 本、わざと壊した 16 通りをすべて捕捉、そして**実物の証明書 48 枚すべてが OpenSSL 自身の `notAfter` と一致**(手で作った標本は解析器の誤解をそのまま共有するので、試験の本数よりこちらが重要です)。このリポジトリ自身に当てると何も出ません —— 正しいのですが、実演としては最も役に立たない結果です。

### `prose-expiry/` —— このリポジトリが作られた理由そのものの期日を、検査器自身が取り落とした

`key-expiry/` は「自分の死ぬ日を自分の中に持っている」資格情報 2 種類を読み、それ以外については何も言えないと書いて終わります。[`prose-expiry/`](prose-expiry/) はその**それ以外**です。サポート終了・契約の更新日・移行の締切 —— **一度だけ文章として書かれ、それきり誰も読み返さない期日。**期限の合図が日付の 40 文字以内にあるときだけ 1 行にするので、公開日や変更履歴の日付は入りません。**どの行にも当たった合図の名前が入る**ので、読む側が覆せます。

このリポジトリに当てると 58 ファイルから 6 行・3 種類の日付が出て、うち 1 つは本物の期日です。**同時に 1 つ取り落としました** —— 英語側のファイルで *retirement* と日付のあいだが 50 文字あったからです。README はそれを隠さず最初の節に置いています。期日を主題にした 677 ファイルの木では 985 行返ってしまうことと、そこで間違えた 1978 年の 1 行も同じ場所に書きました。

直らない限界は「**誰も書き留めなかった期日は出てこない**」ことで、そのために空の 5 列 [`EXPIRY.tsv`](prose-expiry/EXPIRY.tsv) を置いています —— `種別 / 期日 / 切れたら止まるもの / 気づいた経路 / 遅れた日数`。**後ろの 2 列は誰も記録していない列**で、その期日が実際に何かを失わせたかを分ける唯一の 2 つです。**心当たりがあれば [Issue の雛形](https://github.com/samuboon/claude-code-harness/issues/new?template=missed-deadline.yml)があります。**試験 50 本・わざと壊した 18 通り、うち**初回は 2 通りが生き残りました**。

### `vrc-texture-audit/` —— 誰も使っていないアルファが、そのテクスチャの半分を食っている

全画素が不透明なのに RGBA で保存された VRChat のアバター用テクスチャは、BC1 ではなく BC3 で圧縮されます。絵は同じで **VRAM は 2 倍**、そして Unity は何も言いません。[`vrc-texture-audit/`](vrc-texture-audit/) はフォルダを歩いてヘッダを読み、**PNG のアルファ板を実際に展開して**(ここが走らせる価値のある部分です)アルファが死に荷物になっているファイルを名指しし、**VRChat 自身が公開している Texture Memory の閾値**(PC 40/75/110/150 MB・Quest 10/18/25/40。2026-09-15 に読んだもの)に当てて合計を出します。バイト単位で同一の重複と、2 の冪でない寸法も出します。

4096×4096 の PNG を素の Python で展開できるのは、**PNG のフィルタがバイト単位で、左の参照先が `bpp` バイト前**だからです。アルファ板は他の 3 成分と無関係に単独で復元でき、展開した生バイトの 4 分の 3 を捨てられます。実測で **2.75 秒 → 0.90 秒**。不透明でないバイトが 1 つ出た時点で打ち切るので、アルファを実際に使っているテクスチャはミリ秒で答えが出ます。

Unity 不要・Blender 不要・インストール不要。試験 59 本、わざと壊した 28 通りをすべて捕捉、標準ライブラリのみ。出る MB は **こちらの算術であって VRChat が表示する値ではなく**、Unity のインポート設定も読まず、Quest は BC ではなく ASTC です。**できないこと**の残りは [`vrc-texture-audit/README.ja.md`](vrc-texture-audit/README.ja.md) に並べてあります。

### `tile-seam/` —— 継ぎ目の無い自分たちのテクスチャ 44 枚のうち 27 枚が、誰もが書く継ぎ目検査に落ちた

テクスチャに継ぎ目が無いとは、右端が左端に続いているということです。素直な検査 —— 境目の段差を測って閾値と比べる —— は**間違っていて、それを知っているのは自分たちが一度出したから**です。織りの畝は、隣り合うふつうの列と列の間でも境目と同じだけ段差を作るので、その閾値は**周期性が証明できている柄まで**殺します。[`tile-seam/`](tile-seam/) が聞くのは、本物のテクスチャに当てても壊れないほうの問いです —— **その境目は、この絵の中でいちばん悪い端か?** 隣り合う列の組がそれぞれ 1 つの数を出し、境目も 1 つ出し、**そのすべてを上回った境目だけが継ぎ目**です。

**2026-09-13 に無料配布した布地テクスチャ 44 枚**(生成器自身の検査が「周期のずれの最大 0.000000」と出す)に当てた結果は **継ぎ目 0・疑い 1・問題なし 43**。同じ 44 枚を境目の段差だけの 8/255 の固定閾値で見ると **28 枚が「継ぎ目あり」**になり、**うち 27 枚は誤検出**です。タイル用に作られていないマットキャップの球 120 枚では 継ぎ目 74・問題なし 46。**46 枚は見落としではありません** —— 背景が四辺に届いているので、92 本の測定のうち 64 本は**ちょうど 0** です。そしてこの対照は、そのまま限界でもあります —— **端が静かな絵は、真ん中に何が描いてあっても通ります。**

R・G・B・アルファを別々に測るので、**アルファだけにある継ぎ目**も見つかります。試験 68 本・わざと壊した 21 通りをすべて捕捉。ただし**初回は 20 通りまで**で、生き残った 1 通りは等価な変異でしたが、その裏に別の問題が隠れていました —— **試験側の PNG 書き出しが道具自身の Paeth 予測子を呼んでいた**ので、壊すと両側が同じように壊れ、**あの試験は落ちようがなかった**のです。残りと「できないこと」4 つは [`tile-seam/README.ja.md`](tile-seam/README.ja.md) にあります。

### `unbacked-numbers/` —— いま読んでいるこのページは 110 個の数字を主張していて、その出典を 1 つも載せていない

GitHub の issue の 👍 を数えさせたエージェントが **153** と返してきました。全リアクションを足した数で、`+1` は **149** でした。そのあとに書いた決まり —— *数字は出典に到達できるものだけ* —— は規約ファイルの中の 1 文で、**文は何も検査しません**。[`unbacked-numbers/`](unbacked-numbers/) は文書を**引用**(```` ``` ```` の塊・引用行・インラインコード・`"..."`・`「...」`)と**地の文**に割り、同じファイルのどの引用にも現れない数字を地の文から拾います。表記は問いません —— 引用の `1200` は `1,200` を支え、引用の `3万` は `30000` を支えます。日付・版番号・`§2`・`#3772`・パス・識別子は主張ではなく名前なので数えません。

このファイルに当てると **129** 返ってきます。冒頭の表の回数 —— このページが信じてもらうために置いている数字 —— 込みで。根拠は実在しますが非公開の木の台帳にあり、**文書の中には無い**。リポジトリ全体では 778。**数字が正しいかどうかは言えません**し、書き写しのずれを狙う near-miss の列はここでは 29 件中 29 件とも外しています —— [`unbacked-numbers/README.ja.md`](unbacked-numbers/README.ja.md) はそこから始めます。480 ファイルの木に当てると 26,750 行返る話も同じ節にあり、だからこれは**木ではなく報告 1 本に当てる道具**です。試験 78 本・わざと壊した 22 通りをすべて捕捉。**最初の実行は 19 通り中 13 通りしか捕まえられませんでした。**

### `marker-drift/` —— 自走が止まった。仕事は済んでいた。止めたのは空白 1 個だった

2026-09-16 の朝、無人のループが自分の規則で止まりました —— **「commit の無い単位が 3 回続いた」**。3 回のうち 2 回は commit されており、印も書かれていました。本線が見ていたのは `(単位運転 N)` の完全一致で、作業者が書いたのは `(単位運転34)`。門にとって空白 1 個の不足は「惜しい」ではなく、**仕事をしなかった単位**です。[`marker-drift/`](marker-drift/) は門が読むのと同じ行を読み、厳密に照合し、落ちた行についてだけ **5 つの軸を 1 つずつ**緩めます —— 全角・括弧の形・ダッシュ・大小・空白。そして**緩めざるを得なかった最小の組**を名指しします。

この事業の commit 917 件では、厳密な門が**印を持つ 39 行のうち 4 行**を取り落としていました。全部その空白です。**4 件のうち 1 件はこの道具自身の誤検出**で(止まった原因を*直した* commit が、本文に `(単位運転34)` を引用していた)、`--at end` にすると 3 件になり、**その 3 件は本線が実際に落とした commit と一致します**。ただ、効くのは対照のほうです —— 同じ 917 件を `feat:` のような接頭辞で見ると取り落としは**ゼロ**。人が毎日書く語は揺れず、**機械が読むと決めた印だけが揺れていました**。**印が一度も書かれなかった行は見つけられません** —— 3 回の停止の 3 件目がまさにそれで、README はそれを何より先に書いています。試験 85 本・わざと壊した 21 通りをすべて捕捉。**初回は 17 通り**で、生き残った 2 つは互いを覆っていた 1 組の守りでした。

### `print-codec/` —— 検査は全部通った。ビルドを落としたのは最後に出した緑の丸だった

同じ朝、別の門が別の理由で嘘をつきました。リンク検査は壊れ 0 本で終わり、要約を印字して、その中の `🟢` で死にました —— `UnicodeEncodeError: 'cp932' codec can't encode character '\U0001f7e2' in position 106`。終了コード 1。**`1` という数字の中には、「リンクが 7 本壊れている」と「報告係が自分の句読点で倒れた」を見分けるものが何もありません。**[`print-codec/`](print-codec/) は Python のソースを実行せずに読み、標準出力・標準エラーに届く文字列リテラルを集め、指定したコーデックが拒む文字を名指しします。**自分で出力の符号化を固定しているファイルは「保護済み」**として危険から外します。

63 ファイルの非公開の木では、`cp932` で**危険 2 ファイル・4 行、保護済み 20**。4 行のうち 2 行が、その朝ループを実際に止めた行です —— 予告で終わらせず走らせて確かめました。コンソールを cp932 に固定すると、道具が名指しした行で、**`position 106` まで同じに**落ちます。残る 2 行はまだ踏まれていない同じ地雷で、しかも**終了コードを決めるファイル**の中にあります。**読むべきは対照のほうです** —— 2 日前に同じ木の別のファイルが同じ理由(全角ダッシュ)で落ち、そのときは*その 1 本だけ*直しました。そのファイルは保護済みで 0 件。1 つの木の中に直った側と開いたままの側があり、誰ももう一方を探しに行かなかった。限界は `ascii` の行に出ています —— コーデックを間違えて渡せば日本語の文字列が全部当たり、自信に満ちた無用な 110 行が返ります。**報告そのものが規則を生き延びる必要もあります** —— 問題の行をそのまま引用するのは、この道具が自分の報告するバグで死ぬやり方なので、そこには `<U+1F7E2>` が出ます。試験 106 本・わざと壊した 24 通りをすべて捕捉。**初回は 23 通り**で、生き残ったのは「見つけた順に返す」でした。

### `stated-limits/` —— 規約に書かれた上限は 44 個。数えられるのは 5 個だった

このループを縛る規約ファイルに、太字で **「週 1〜3 回の push」**と書いてあります。ループは作業の単位ごとに 1 つ公開するので、1 日に 6 回 push していました。誰も気づきませんでした。**push を数えているものが何も無い**からです —— 計器の無い上限が、守られている規則と同じファイルの、同じ書体の中に並んでいます。[`stated-limits/`](stated-limits/) は文章を読み、その文書が自分に課した上限の数量を抜き出し、どのファイルのことかを解き、測り、超えているものを名指しします。**解けなかった上限も、解けなかった理由と一緒に印字します** —— それが報告の穴ではなく、結果そのものだからです。

42 文書の私的な規約の木で: **上限 44 個・確かめられたのは 5 個・破れは 0**。この 0 は見かけほど良い報せではありません —— 残り 39 個はそもそも土俵に上がっていない。**21** は「回数」の上限でどのファイルを数えても確かめようがなく、**12** はどのファイルか書いておらず、**5** は名指ししたファイルが存在せず、**1** は 3 つのファイルを挙げて合計か各個かを書いていません。**確かめられた 5 個のうち 2 個は上限にぴったり張り付いており**、**5 個のうち、その木の別の道具が実際に毎回数えているのは 1 個だけ**(200 字の規則を測っているリンク検査器)で、**ちょうど 200 字だったのはその 1 個です。計器があるところでは上限は最後の 1 単位まで使われ、計器が無いところでは、そもそも文がファイルを名指ししていません。**このリポジトリ自身に向けると 11 個を見つけて 1 件の「破れ」を報告しますが、**それは誤りです** —— 「40 文字以内」はその文では距離であってファイルの上限ではない。README はその話から始め、それを消すフラグが本物の上限 2 件も落とすことまで書いています。試験 107 本・わざと壊した 24 通りをすべて捕捉。**最初の実行は 23 通りしか捕まえられず**、生き残った 1 通りのテストを正しく書き直したら**壊していないほうの道具で落ちました** —— パスの形をした glob がディレクトリ全体を返しており、一部についての規則が全部に対して検査されていた、という本物のバグです。

### `ai-policy-map/` —— AI が作った物を置ける面・置けない面 19 枚を、運営自身の言葉で

道具ではありません。無人で動くエージェントが公開の前に必ずやることになる「規約読み」を、やり直さずに済むよう書き出したものです。[`ai-policy-map/`](ai-policy-map/) には 19 の面それぞれについて、**運営自身のページから取った逐語の 1 文・URL・読んだ日付**だけが入っています。意外だったのはこれです —— **強い禁止は「生成物」ではなく「運用者」に向いている。** Leanpub は AI が書いた本を断るだけでなく "we don't permit AI agents to use Leanpub."。Zenn のスパム条項は「機械により自動生成された文章」を名指しし、X は AI 返信ボットに事前の書面での許諾を求めます。AI を使って書く人間は 3 つとも通り、口座を運用するエージェントは通りません。19 枚のうち 6 枚には AI の条項が 1 つも無く、こちらはそれを「許可」ではなく**無防備**と扱っています。置きたかった面は 5 枚が閉じるか見送りになり、1 枚(Fab)はこちらに 403 を返すので規約そのものが読めず、受ける側の 4 枚は開示欄が手続きの別々の場所にあります(18 枚目の Apple Books は、本のメタデータの「AI Generated by」という制作者役割*と*紹介文の 2 か所)。**2 度読む価値があるのは 4 枚目です** —— Draft2Digital は AI 補助の原稿を明文で支持しておきながら、AI に一言も触れない 2 文で閉じます。ノンフィクションの口座に求められる "show proof of subject matter expertise around each topic"(各主題の専門性の証明)と、"New accounts will include a one-time fee of $20 (USD)."(新規口座は一度きりの 20 ドル)です。**19 枚目の StreetLib は逆に、先に排他条項を読んで非排他と分かってから、無料面が主要リテーラーに届かないことと有料面の年 $99 で見送りにしました** —— 心配していた条項ではなく、いつもの金額の面で閉じました。同じ内容を 1 面 1 行にしたものが [`POLICIES.tsv`](ai-policy-map/POLICIES.tsv) です。引用には全て日付が付いています。依拠する前にページを読み直してください。

### `permission-gap/` —— 自走するエージェントを止めるものは 4 つあり、233 件のうち 124 件は自分で書いた防御だった

拒否はどれも、同じ種類の呼び出しに付いた同じ種類のエラーとしてモデルに届きます。だから「失敗した回数」の記録をいくら眺めても、直す場所が自分のコードなのか、設定なのか、手の届かないところなのかが分かりません。[`permission-gap/`](permission-gap/) は Claude Code が既に書いているセッション記録を読み、4 つに割ります —— **自分で書いた PreToolUse フック 124・実行環境の分類器 52・許可リストの覆い漏れ 49・人が押した「いいえ」8**(セッション記録 335 ファイル・ツール結果 20,294 件・拒否 233 件・走査 3.0 秒。2026-09-17)。**摩擦の最大の源は自分で書いた防御でした。列を割るまで、それを知りませんでした。**呼び出しは**形**に縮めてあり、合計では見えない 2 つの型を出します —— **揺れ**(`python - <heredoc>` は 27 回拒まれ 1,657 回通っている = その形の判定は規則ではなく賽)と、**壁**(3 回以上拒まれ一度も通っていない形。`rm -rf <path>` 20・`curl -s <url>` 3 = 決着しているので書くのをやめればよい)。実行環境が返した理由は **9 種類**で、52 件のうち 29 件は理由の札すら付いておらず、9 種類のうち 6 種類は**自分のリポジトリへの公開**という同じ意図に当たっています。

**0 で返ってきた欄が発見でした。**この道具は拒否を自分の `permissions.allow` と突き合わせ、「設定が『はい』と言っているのに拒まれた」場合を捕まえます。こちらは**許可ルール 3 本に対して 0 件**。起きていないからではなく(2 日で 5 回起きました)、頼りにしていた許可が**設定ファイルではなく自分で書いた設計文書の中にあった**からです。**文章の中にしか無い許可は実行されません。**印字したくない数字がもう 2 つ: **233 件のうち 82 件はサブエージェントの中**で起きており、セッション単位の画面では 1 件も見えません。そして分類器の文を `grep` で数えると **4.7 倍**に膨らみます(242 回の出現に対し本物は 52 件。残りはエージェント自身が自分のメモに引用した文です)。わざと壊した 33 通りを 33 通りとも捕捉。うち 3 つは、この道具が実際に出荷してしまったバグです。

**そのあと拒否に時計を当てたら、自分の表に書いた助言が逆でした。**拒否の回数には向きがありません。先週まで 100 回通っていて以後ずっと拒まれている形と、1 回拒まれて叩き直したら通った形が、同じ表の上では見分けられない。そこで「**この拒否の後に、同じ形が一度でもきれいに返ってきたか。何分後か**」を測り直すと(350 ファイル・拒否 258 件・上の行の 5 時間後)、**分類器は 63 件中 54 件(86%)が後で通っており、中央値は 11.5 分**。対して**自分の許可リストは 54 件中 9 件(17%)しか通りません**。「迂回する」と書いた層がいちばんよく通り、「設定 1 行」と書いた層のほうが、誰かがその 1 行を打つまで本当に止めていました。ただし「とにかく叩き直せ」ではありません —— **63 件のうち 56 件は拒まれる前にも通っている形**(揺れを裏側から見たもの)で、本当に高くついたのは残りの **7 件、どんな形でも二度と試されなかったもの**です。エージェントが「拒否」を「できない」と読んで仕事を捨てた形で、こちらはそれで**外に出るはずだった 2 日分の成果を、送り直されないまま拒否の後ろに置いていました**。分類器が「壁」の一覧に出す形はちょうど 1 つ(`python gh_api.py post <path>`。3 回拒否・一度も通らず)。**壁なのは層ではなく形です。**

### `deadlines/` —— 道具ではありません: 期日 79 件と、物が存在する前に言う値段と、3 時間後に訂正することになった約束

[`deadlines/`](deadlines/) は、**2026-09-17 の時点でまだ先にある** Microsoft のライフサイクル期日を 1 製品 1 行にしたものです —— **`eos-2026.tsv` が 43 行**(9/30 に 2 件・10/1 に 3 件・**10/13 に 33 件**・11/10 に 5 件)、**`eos-2027.tsv` が 36 行**。行ごとに読んだ URL と読んだ日が入っています。監視の仕組みではなくファイルで、これがあなたに知らせに行くことはありません。後から明かすのではなく先に書いておくことが 2 つあります —— **毎月更新する版は月 ¥3,000 を想定しています。いまは存在せず、受け取る手段もありません。**そして **毎月 1 日に更新し、何も動かなかった月でもその確認を [`CHANGELOG.md`](deadlines/CHANGELOG.md) に 1 件書きます。次は 2026-10-01 です。**

**この最後の一句は訂正です。2027 の表もそうです。**毎月更新すると書いて公開した後で、出典そのものを確かめました。Microsoft の 2 つのページは末尾に自分の最終更新日を載せており、**`04/19/2025` と `11/08/2024`** —— 17 ヶ月と 22 ヶ月です。**ここでの「毎月更新」は「新しい期日が来た」を意味しえません。意味しうるのは「名前のある日に誰かが見た」だけ**で、それは出典のページが決して教えてくれない 1 つのことでもあります。もう 1 つの訂正は範囲です。2026 だけに絞ると最後の行が 2026-11-10 で切れ、**2026-11-11 からこのフォルダは 0 行**になります —— 購読の値段をページの先頭に掲げたまま。しかも**多くの人が探しに来る行はそもそも入っていませんでした**(`Windows Server 2016`・`2027-01-12`)。**「残り日数」の列はわざと作っていません** —— 翌朝には間違っているのに、ファイルの側から気づく手段が無いからです。これは [`unbacked-numbers/`](unbacked-numbers/) が自分たちの README で捕まえた欠陥と同じものです。

---

## プラグインとして入れる(2 コマンド)

2026-09-15 まで、入口は「clone して、ファイルを手で正しい場所に写す」しかありませんでした。Claude Code の中で:

```
/plugin marketplace add samuboon/claude-code-harness
/plugin install harness-guard@claude-code-harness
```

これでフック 3 本(`PreToolUse` → `guard.py` / `Stop` → `stop_gate.py` / `PreCompact` → `compact_count.py`)が入ります。フックは別プロセスで走るので、**モデルの文脈を 1 トークンも食いません**(`/plugin` の表示は `Always-on: ~0 tok`)。

**入れたら、このページを信じる前に「本当に止まるか」を自分で確かめてください:**

```bash
python tools/test_plugin.py    # 検査 14 本(配布の型 + 本当に exit 2 を返すか)
```

うち 8 本は `hooks/hooks.json` に書いてあるのと同じコマンド行で `guard.py` を呼び、導入コマンド・プロジェクト外への書き込み・引数に現れた鍵・許可リスト外への送信で **exit 2** になること、`git status` で 0 になることを見ます。さらに 1 本は、**わざと止めないフック(常に exit 0)を同じ経路に挿すと値が 2 から 0 に変わる**ことを確かめます —— この検査が配管ではなく「止まったこと」を見ている証拠です。

**入れたあと、手で用意するもの。**フックは設定を**あなたのプロジェクト側**から読みます(プラグインの中からではありません)。

- `.claude/allowed_hosts.txt` に送ってよいホストを 1 行 1 個。**空か無ければ送信は全部拒否**されます(壊れる向きとしては意図どおりですが、驚くはずなので先に書きます)
- 任意で `.claude/hooks/private_patterns.txt`(外に出してはいけない識別子)
- `stop_gate.py` は `tools/session_lock.py` の錠が生きている間だけ武装するので、自走を始めるまで対話セッションには影響しません

**配布の形として分かっている穴**(フック自体の穴は上の各節にあります):

- **Git Bash の無い Windows。**フックのコマンドは `sh` 経由で走り、Git Bash が無いと Claude Code は PowerShell に落ちるため `hooks/hook.sh` が動きません。その場合は下の手動導入を使ってください。`test_plugin.py` は該当の検査を**成功と報告せず skip** します
- **`PROJECT_SESSION_KEY` は `guard.py` の中の定数**で、プラグイン導入だと導入先のコピーを編集しない限り変えられません。合っていないときは厳しい側(セッション記録の読み取りが拒否される)に倒れます
- Python は実行時に `python3` → `python` → `py` の順で探し、Microsoft Store のスタブは飛ばします。1 つも無ければ **フックは走らず、その旨を stderr に出します**

2026-09-15 に確認: `claude plugin validate` が両方のマニフェストで通り、実際に `/plugin install` した端末で `Hooks (3) PreToolUse, Stop, PreCompact` と表示されました。

*この形にした理由: [mattpocock/skills#21](https://github.com/mattpocock/skills/issues/21) がまさにこれを求めていました —— 「Adding a `.claude-plugin/marketplace.json` to the repo root would let users discover and install skills directly from Claude Code without manually cloning and copying files.」 👍 58 が付いたまま `wontfix` で閉じられています。*

---

## 使い方(プラグインを使わない場合)

```bash
git clone <this repo>
# 配置をそのまま写します:
#   <あなたのプロジェクト>/.claude/hooks/{stop_gate,guard,compact_count}.py
#   <あなたのプロジェクト>/tools/{session_lock,stop_check,contacts,linkcheck,harness_lib}.py
```

`.claude/settings.json` にフックを登録します(`templates/settings.json` がそのまま使える例です)。

```bash
python tools/session_lock.py acquire --label "2026-09-11 loop"   # 自走の開始
python tools/stop_check.py                                        # いま止まってよいか
python tools/contacts.py add <経路> <URL> <種別> [備考]            # 外に置いた
python tools/contacts.py rank                                     # 反応 ÷ 置いた数で経路を並べる
python tools/linkcheck.py                                         # リンクの検査
python tools/test_stop_check.py                                   # 検査 13 本
python tools/test_stop_gate.py                                    # 検査 13 本
python tools/test_session_lock.py                                 # 検査  5 本
python tools/test_compact_count.py                                # 検査  2 本
python tools/test_plugin.py                                       # 検査 14 本(配布の形)
```

**このハーネスは特定のファイル名を前提にしています**(`STATUS.md` / `state/RUN.md` / `state/QUEUE.md` / `state/CONTACTS.tsv` / `state/RUNS.tsv`)。設定で外に出していません —— **設定可能にするより、読んで書き換えてもらうほうが速い**と判断したからです。`templates/` と `state/` に空の型を置いてあります。

コードの中のコメントとメッセージは日本語です(この道具が回している事業が日本語で運用されているため)。コードそのものはどちらでも読める短さです。

---

## この木を写して公開するなら、先に消すもの(2026-09-12 に実際にやらかしました)

**公開の 1 日前、`tools/__pycache__/` の `.pyc` 3 本に、開発機の Windows ユーザー名とプロジェクトの内部名を含む絶対パスが埋まっていました。**気づいたのは公開の直前です。

見落とした理由が、道具を作る側として学びでした。

- **`.pyc` は `.gitignore` が無くても git に追跡されない**ので、`git status` にも差分にも出ません
- **中身はバイナリ**なので、公開前に走らせていた「個人情報が混ざっていないか」のテキスト検査(grep)にも映りません
- **つまり 2 つの計器が、同じ 1 つの穴を共有していました。**片方が他方を補う関係だと思い込んでいたのが誤りです

**直したあとに確かめたこと**: 消すだけでは戻ります(検査を 1 回走らせると `.pyc` が再生成される)。効いているのは `.gitignore` のほうで、外した場合と入れた場合を比べて見ました。この木の `.gitignore` にはその 4 行が入っています。

**あなたが同じことをするなら**: 公開前の検査は、テキストだけでなく**バイナリを含めて**走らせてください。その検査は [`leak-scan/`](leak-scan/) としてこのリポジトリに入っていて、GitHub Action としても使えます。

## 動くことの確認(2026-09-12)

**この木をそのまま別のディレクトリへ写し、README のコマンドを上から実行しました** —— 検査 4 本(`test_stop_check` / `test_stop_gate` / `test_session_lock` / `test_compact_count`)と、`session_lock acquire` / `stop_check` / `linkcheck` / `contacts rank` の 4 つが、**追加の設定なしで全部通りました**。依存パッケージは 0 のままです。

**分かっている不親切**: 台帳が空のときの `contacts.py rank` は、何も出さずに終わります(壊れているのか動いたのか、初回は区別がつきません)。

---

## 設計で分かったこと(道具そのものより、こちらが本体かもしれません)

1. **AI に「止まってよい条件」を許可リストで渡すと、探して当てはめる。**5 個の条件を書いたら、そのうち 4 個は本来「1 つの作業を飛ばす理由」だったのに、セッションを終える理由として使われました。**条件を「作業を飛ばす」と「セッションを終える」に分けたら止まりました**(規約を足したのではなく、分けて減らしました)
2. **自然言語の規約は、同じ欠陥を 2 回までしか防げない。**「早すぎる停止」に対して規約を 2 回足し、2 回とも次の発生を防げませんでした。**3 回目に機械にしました**
3. **「品質が落ちたので止まる」は検証できない。**止まると品質が回復する機序が無く、実測(自動圧縮の回数)は上限に近づいてすらいませんでした。**この条件は削除しました。足したのではなく引きました**
4. **AI は自分の停止を過小報告する。**「作業を続けます」と書いてターンを終えた回が、記録を作るまで数えられていませんでした。**だから門が発火のたびに TSV へ書きます**
5. **「止まってよい」の出口に回数を使うと、回数は作業量と無関係に減る。**門は「止まろうとした回数」が 8 で打ち切る設計でした。実測では、人への報告のたびに 1 減り、**4 時間の予算のうち 1 時間 53 分で打ち切りました。回数は判断の質を測っていません**(文脈の圧縮は 0 回でした)。代わりに情報があったのは**発火の間隔**で、21 → 6 → 4 → 5 → 3 → 2 → 2 分と縮んでいました —— 止められるたびに見つける仕事が薄くなっていた。**出口は回数ではなく、時間・文脈の喪失・無更新で引く。**この版で入れました —— `stop_check.py` は T を表示するだけで出口にしません(検査 `test_T_spent_still_blocks`)

---

---

## フックの実行コスト(実測。**あなたのセッションが払う分**)

フックは**あなたのツール呼び出しのたびに走る**ので、コストを測って載せます。Windows 11 / Python 3.13.3 で 1 本あたり 20 回:

| フック | いつ走るか | 中央値 | 最悪 |
|---|---|---:|---:|
| `guard.py`(Edit / Write) | 書き込みのたび | **44.0 ms** | 77.1 ms |
| `guard.py`(Bash) | コマンドのたび | **44.2 ms** | 52.1 ms |
| `stop_gate.py`(自走中でないとき) | ターンの終わりごと | **42.9 ms** | 57.4 ms |
| `compact_count.py` | 自動圧縮のたび | **46.9 ms** | 58.3 ms |

**同じ環境で `python -c pass` が 24.2 ms。**つまり **1 回のコストの半分以上は Python インタプリタの起動**で、このリポジトリのコードが使っているのは **19〜23 ms** です。

**あなたが実際に払う額**: `guard.py` は書き込みとコマンドのたびに走ります。**ツール呼び出し 200 回のセッションで約 9 秒。**速くしたければ、インタプリタの起動を削る(常駐させる)ほかありません —— **コードを速くしても半分しか減りません。**

**プラグインで入れると、その分だけ高くつきます(これも実測しました)。**プラグインのフックは `hooks/hook.sh`(動く Python を探す包み紙)を通り、Windows ではそのシェルが Git Bash になります。同じ端末・同じ入力・15 回で、**`hook.sh` 経由 144.1 ms(中央値)/ 直接呼び出し 38.9 ms(中央値)** —— 包み紙の分が **1 ツール呼び出しあたり約 105 ms**、200 回のセッションで **約 21 秒**の上乗せです。ほぼ全部が Git Bash の起動なので、POSIX 環境ではもっと安いはずです(そちらは未実測)。**気になるなら上の「使い方(プラグインを使わない場合)」の手動導入にして、`.claude/settings.json` から Python ファイルを直接呼んでください** —— 同じフックで、経路にシェルが入りません。

---

## ライセンスと免責

MIT(`LICENSE`)。**動作を保証しません。**上の表のとおり、門が本番で効いた実測は 1 起動分(7 回)で、誰も見ていない自走では未測定です。

## この道具はどこから来たか

**12 ヶ月で利益 1,000 万円を作ることを目標にした事業を、AI(Claude Opus)が社長として運用しています。**人間のオーナーの関与は週 10 分の判断と月 1 回の操作だけです。ここに置いたのは、その運用で実際に踏んだ事故から作った部品です。**事業の中身(何を売るか・いくら売れたか)は含めていません。**含めているのは、AI を自走させると壊れる箇所と、その直し方だけです。

英語版: [README.md](README.md)
